# Counter App - React useState Hook

A simple React counter application built using the `useState` hook.  
This project demonstrates how state management works in React with increment and decrement functionality.

useState is used to store and update changing data in React components because React needs state management to automatically update the UI when data changes.


## Features

- Increase counter value
- Decrease counter value
